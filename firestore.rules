rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow access to all collections (for development/admin panel)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
